export type LessonMapButton = {
  num: number;
  color: string;
  left: number;
  top: number;
};

export const lessonMapButtons: LessonMapButton[] = [
  // Add Pattaya pack lesson buttons here when this pack gets a map screen.
  // Example:
  // { num: 1, color: '#264653', left: 12, top: 68 },
];